cmake_minimum_required(VERSION 3.16)

set(PREMAKE_BUILD_TYPES "Debug" "Release")
get_property(multi_config GLOBAL PROPERTY GENERATOR_IS_MULTI_CONFIG)
if(multi_config)
  set(CMAKE_CONFIGURATION_TYPES "${PREMAKE_BUILD_TYPES}" CACHE STRING "list of supported configuration types" FORCE)
else()
  set(CMAKE_BUILD_TYPE "Debug" CACHE STRING "Build Type of the project.")
  set_property(CACHE CMAKE_BUILD_TYPE PROPERTY STRINGS "${PREMAKE_BUILD_TYPES}")
  if(NOT CMAKE_BUILD_TYPE IN_LIST PREMAKE_BUILD_TYPES)
    message(FATAL_ERROR
      "Invalid build type '${CMAKE_BUILD_TYPE}'.
      CMAKE_BUILD_TYPE must be any one of the possible values:
      ${PREMAKE_BUILD_TYPES}"
    )
  endif()
endif()

set(CMAKE_MSVC_RUNTIME_LIBRARY "")
set(CMAKE_C_FLAGS "")
set(CMAKE_CXX_FLAGS "")
set(CMAKE_C_FLAGS_DEBUG "")
set(CMAKE_CXX_FLAGS_DEBUG "")
set(CMAKE_C_FLAGS_RELEASE "")
set(CMAKE_CXX_FLAGS_RELEASE "")

project("Portfolio")
include(bindless.cmake)
include(bubble.cmake)
include(cellShading.cmake)
include(cheapBlur.cmake)
include(chromaticAbberation.cmake)
include(computeTest.cmake)
include(contrast.cmake)
include(dilation.cmake)
include(edgeDetection.cmake)
include(erosion.cmake)
include(frost.cmake)
include(gameOfLife.cmake)
include(gamma.cmake)
include(gaussian.cmake)
include(gaussianMulti.cmake)
include(golCompute.cmake)
include(heatHaze.cmake)
include(mipMapping.cmake)
include(parallax.cmake)
include(perlin3D.cmake)
include(perlinNoise.cmake)
include(pixelize.cmake)
include(radialBlur.cmake)
include(scene.cmake)
include(sepia.cmake)
include(sharpen.cmake)
include(textureSettings.cmake)
include(textured.cmake)