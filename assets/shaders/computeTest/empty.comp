#version 450 core

layout(local_size_x = 4) in;

layout (binding = 0) buffer testBuffer
{
    vec4 data;
};

void main(void)
{
    //basic math operation to test compute shader functionality
    data[gl_LocalInvocationID.x] += gl_LocalInvocationID.x * 0.1f;    
}